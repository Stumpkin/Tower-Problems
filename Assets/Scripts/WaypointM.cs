using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WaypointM : MonoBehaviour
{
    // Start is called before the first frame update
    public int size = 3;
    public List<WayPoint> waypoints;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
